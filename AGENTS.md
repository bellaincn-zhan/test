# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently empty (contains only a placeholder `README.md`). There are no services, dependencies, build steps, lint checks, or tests to run. Future agents should re-evaluate once source code is added.
